class PagesPagination {
  const PagesPagination({
    required this.first,
    required this.next,
    required this.prev,
    required this.last,
  });

  final int? first;
  final int? next;
  final int? prev;
  final int? last;
}
